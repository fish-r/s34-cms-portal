'use client'

import { useMantineTheme, Group, rem, Button, Text } from "@mantine/core";

export const StorylineEditor = (props: React.PropsWithChildren) => {
    return (
    <>
        <Text>Storyline Editor</Text>
    </>
  )
}